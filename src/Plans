1. realise nextCard() method in Dictionary class. Perhaps need a counter or just use a stack for run

2. Make model
   - create a Facade pattern to deal with interface
   - create an Observer pattern

3. Make GUI (in progress)

4. Divide the GUI into a view and a controller

5. !! в классе CardBuilder сконектить метод saveFile(File file) с DataManger !!

6. Разбить работу GUI по модулям View and Controller

7.  DONE!Сделать классы базовых GUI во view;
    done! UserSelectGUI;
    done! UserProfileGui;
    done! progress! TrainingGUI;
    done! progress DictionaryGUI;
    done! progress CardBuilderGUI;

9. Добавить слушателей для всех интерактивных объектов во всех базовых GUI:
    - UserSelectGUI;
    - UserProfileGui;
    - TrainingGUI;
    - CurrentDictionaryGUI;
    - CreateNewCardGUI;

8. Добавит логику в слушателей во всех базовых классах GUI в модуле view
    - UserSelectGUI;
    - UserProfileGui;
    - TrainingGUI;
    - CurrentDictionaryGUI;
    - CreateNewCardGUI;

9. DONE! Сделать рефакторинг GUI:
   done! сделать отдельный BasicGUI с фреймом, меню бар и главной панелью
   done! унаследовать все GUI от BasicGUI
   done! UserSelectGUI;
   done! UserProfileGui;
   done! TrainingGUI;
   done! DictionaryGUI;
   done! CardBuilderGUI;