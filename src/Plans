1. realise nextCard() method in Dictionary class. Perhaps need a counter or gust use a stack for run

2. Make model
   - create a Facade pattern to deal with interface
   - create an Observer pattern

3. Make interface

4. Divide the interface into a view and a controller